# download_3d_ken_burns
~~~
git clone git@github.com:Binyr/download_3d_ken_burns.git
python download.py
python unzip.py

# ensure all the file are unziped correctly.
# if some file is error, unzip it manully.
# cd downloads && unzip -q -n {zip_name} -d {dir_name}
cat Error_file_names.json 
~~~